/**
 * A list of available themes for the application.
 *
 * Note: If you update this list, ensure that you also update the corresponding
 * theme configurations in [`globals.css`](./globals.css) for daisyUI themes to maintain consistency.
 */
const themes = ["emerald", "dark", "valentine", "retro", "cupcake", "dracula", "dim", "corporate", "fantasy"];

export default themes;
